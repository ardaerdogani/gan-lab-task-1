# Base dependencies only.
# Install PyTorch separately so shared CUDA hosts can use the matching wheel,
# for example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
numpy
scipy
scikit-learn
matplotlib
pillow
