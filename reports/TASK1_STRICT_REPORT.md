# Task 1 Report (Minimal Academic Alignment)

## Introduction

This task evaluates whether synthetic fruit images generated by a Conditional GAN can improve classifier performance, and how this effect changes with training size.

Core question:
**Does synthetic data help classification, and at which training sizes?**

## GAN Implementation

- GAN type: Conditional GAN (trained from scratch, no transfer learning)
- Single preset: `BCE + balanced sampler + uniform fake labels`
- Training data: `data/split/train`
- Synthetic generation target:
  - A sufficiently large synthetic pool to support max count `1600`
  - Implemented as `600` per class (`1800` total), which provides margin for stratified sampling

### Generated Samples

Add representative generated image grids from the final run:
- `runs_gan/task1_main/samples_epoch_*.png`

### FID (Overall Only)

Report one overall FID value from:
- `reports/fid_task1.json`

## Classification Comparison

Three required scenarios:
1. Only Synthetic (anchor at `1600`)
2. Only Real
3. Real + Synthetic (`N real + N synth`)

Primary table source:
- `reports/task2_three_case_comparison.md`

## Data Size Trend Analysis

Training sizes:
- `200`, `400`, `800`, `1600`

For each size, evaluate:
- Accuracy
- Macro F1
- Training time

Use figures:
- `reports/figures/performance_vs_training_size.png`
- `reports/figures/training_time_vs_training_size.png`

Include short interpretation for:
- `real_only` trend
- `real_plus_synth` trend
- where synthetic data helps most

## Conclusion

Provide a direct answer to:
**Does synthetic data help, and at which training sizes?**

Expected structure:
1. When synthetic data improves results
2. Where gain is small/neutral
3. Practical implication for low-data regimes
